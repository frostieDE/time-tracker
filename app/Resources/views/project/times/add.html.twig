{% extends "main.html.twig" %}

{% block title %}{{ 'project.times.add.header'|trans }}{% endblock %}

{% block breadcrumb %}
    <ol class="breadcrumb">
        <li><a href="{{ path('dashboard') }}">{{ 'dashboard.index'|trans }}</a></li>
        <li><a href="{{ path('show_project', { slug: project.slug }) }}">{{ project.name }}</a></li>
        <li><a href="{{ path('show_times', { slug: project.slug }) }}">{{ 'project.times.show.header'|trans }}</a></li>
        <li class="active">{{ 'project.times.add.header'|trans }}</li>
    </ol>
{% endblock %}

{% block content %}
    <h3><i class="fa fa-clock-o"></i> {{ 'project.times.add.header'|trans }}</h3>

    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}

    <div class="form-buttons">
        <button class="btn btn-primary">
            <i class="fa fa-plus"></i> {{ 'project.times.add.submit'|trans }}
        </button>
    </div>
    {{ form_end(form) }}
{% endblock %}